{
  "buildCommand": "expo export --platform web",
  "outputDirectory": "dist",
  "installCommand": "npm install && npm install -g serve",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
